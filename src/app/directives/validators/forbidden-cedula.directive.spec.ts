import { ForbiddenCedulaDirective } from './forbidden-cedula.directive';

describe('ForbiddenCedulaDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbiddenCedulaDirective();
    expect(directive).toBeTruthy();
  });
});
