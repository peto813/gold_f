import { WeightConvertPipe } from './weight-convert.pipe';

describe('WeightConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new WeightConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
